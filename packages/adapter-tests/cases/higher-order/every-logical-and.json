{
  "description": "renders every() with logical AND predicate",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "items().every(t => t.price > 100 && t.active)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{$bf_result := true}}{{range .Items}}{{if not (and (gt .Price 100) (.Active))}}{{$bf_result = false}}{{break}}{{end}}{{end}}{{$bf_result}}"
    },
    "hono": {
      "template": "{items().every(t => t.price > 100 && t.active)}"
    }
  }
}
