{
  "description": "renders find() with complex predicate using template iteration",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "items().find(t => t.price > 100)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{range .Items}}{{if gt .Price 100}}{{.}}{{break}}{{end}}{{end}}"
    },
    "hono": {
      "template": "{items().find(t => t.price > 100)}"
    }
  }
}
