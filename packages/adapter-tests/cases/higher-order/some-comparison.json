{
  "description": "renders some() with comparison predicate using range+variable",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "items().some(t => t.price > 100)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{$bf_result := false}}{{range .Items}}{{if gt .Price 100}}{{$bf_result = true}}{{break}}{{end}}{{end}}{{$bf_result}}"
    },
    "hono": {
      "template": "{items().some(t => t.price > 100)}"
    }
  }
}
