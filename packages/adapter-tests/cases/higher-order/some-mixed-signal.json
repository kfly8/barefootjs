{
  "description": "renders some() with mixed predicate and signal reference",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "items().some(t => t.price > minPrice() && t.category === type())",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{$bf_result := false}}{{range .Items}}{{if and (gt .Price $.MinPrice) (eq .Category $.Type)}}{{$bf_result = true}}{{break}}{{end}}{{end}}{{$bf_result}}"
    },
    "hono": {
      "template": "{items().some(t => t.price > minPrice() && t.category === type())}"
    }
  }
}
