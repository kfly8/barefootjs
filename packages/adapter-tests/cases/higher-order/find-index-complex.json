{
  "description": "renders findIndex() with complex predicate using template iteration",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "items().findIndex(t => t.price > 100)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{range $i, $_ := .Items}}{{if gt .Price 100}}{{$i}}{{break}}{{end}}{{end}}"
    },
    "hono": {
      "template": "{items().findIndex(t => t.price > 100)}"
    }
  }
}
