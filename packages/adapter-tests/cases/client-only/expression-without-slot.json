{
  "description": "renders client-only expression without slotId",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "todos().every(t => t.done)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null,
    "clientOnly": true
  },
  "expected": {
    "go-template": { "template": "" },
    "hono": { "template": "{todos().every(t => t.done)}" }
  }
}
