{
  "description": "renders filter().length expression",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "todos().filter(t => !t.done).length",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{len (bf_filter .Todos \"Done\" false)}}"
    },
    "hono": {
      "template": "{todos().filter(t => !t.done).length}"
    }
  }
}
