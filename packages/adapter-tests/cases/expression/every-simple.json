{
  "description": "renders every() with simple boolean predicate",
  "level": "method",
  "method": "renderExpression",
  "input": {
    "type": "expression",
    "expr": "todos().every(t => t.done)",
    "typeInfo": null,
    "reactive": false,
    "slotId": null
  },
  "assertions": [
    {
      "type": "output-not-empty"
    }
  ],
  "expected": {
    "go-template": {
      "template": "{{bf_every .Todos \"Done\"}}"
    },
    "hono": {
      "template": "{todos().every(t => t.done)}"
    }
  }
}
